name: publish website
on: [push]

jobs:
  copy-file:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Pushes test folder
      uses: datalbry/copy_folder_to_another_repo_action@1.0.0
      env:
        API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
      with:
        source_folder: 'website'
        destination_repo: 'padovagrandtour/padovagrandtour.github.io'
        destination_folder: ''
        user_email: 'luca.fabbian.1999@gmail.com'
        user_name: 'lucafabbian'
        commit_msg: '[GHA] Update the website.'
